public class Rock : BossObject
{
    // Start is called before the first frame update
    new void Start()
    {
        setParent = true;
        base.Start();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
